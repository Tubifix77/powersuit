# suit_cloud_gateway: Node 8 <-> Node 9 link client (docs/link-protocol.md).
# ws_client / link_monitor / uplink_batcher / offline_queue are ROS-free;
# gateway_node + downlink_router import rclpy and are entry-point-only.
